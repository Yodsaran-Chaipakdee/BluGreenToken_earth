{
  "private": true,
  "name": "blugreentoken-earth",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "static:dev": "python -m http.server 5179 --bind 127.0.0.1",
    "worker:dev": "wrangler dev --local --host 127.0.0.1 --port 5179",
    "smoke": "node tests/api-smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
